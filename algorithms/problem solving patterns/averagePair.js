// Given a sorted array of integers and a target average
// determine if there is a pair of values where the
// average equals the target average. More than one pair may match.

const averagePair = (arr, targetAvg) => {

}